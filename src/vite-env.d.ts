/// <reference types="vite/client" />

// Injected by vite.config.ts so the frontend can read the app version from
// a single source of truth (package.json) instead of a hardcoded string.
declare const __APP_VERSION__: string;
